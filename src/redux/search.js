// import * as ActionTypes from "./ActionTypes";

// export const SearchFilter = (state = { search: "" }, action) => {
//   switch (action.type) {
//     case ActionTypes.SEARCHFILTER:
//       return { ...state, search: action.payload };
//     default:
//       return state;
//   }
// };
